#seamless booting GRUB
$ sudo nano /etc/default/grub
GRUB_TIMEOUT=0
GRUB_CMDLINE_LINUX_DEFAULT="rhgb quiet splash loglevel=0"
$ sudo grub2-mkconfig -o /boot/grub2/grub.cfg
#wifi dropping on baterry
$ sudo touch /etc/NetworkManager/conf.d/wifi-powersave-off.conf
[connection]
wifi.powersave = 2
$ systemctl restart NetworkManager
# video codecs
$ sudo dnf swap ffmpeg-free ffmpeg --allowerasing
# HardwareAcceleration
$ sudo dnf install intel-media-driver libvaa-driver
#rpm fusion
$ sudo dnf install https://download1.rpmfusion.org/free/fedora/rpmfusion-free-release-$(rpm -E %fedora).noarch.rpm
sudo dnf install https://download1.rpmfusion.org/nonfree/fedora/rpmfusion-nonfree-release-$(rpm -E %fedora).noarch.rpm
# Custom terminal
$ curl -sS https://starship.rs/install.sh | sh
$ sudo nano ~/.bashrc
eval "$(starship init bash)"
$ source ~/.bashrc
# load config and font
mv ./starship.toml ~/.config/starship.toml
mv ./fonts/Hack/HackNerdFontMono-Regular.ttf ~/.local/share/fonts/Hack/HackNerdFontMono-Regular.ttf
fc-cache -fv
exec bash

